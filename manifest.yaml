#© 2021 Sean Murdock

apiVersion: apps/v1
kind: Deployment
metadata:
  name: michael-stedi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: michael-stedi
  template:
    metadata:
      labels:
        app: michael-stedi
    spec:
      containers:
        - name: michael-stedi
          image: gcr.io/gcr.io/$PROJECT_ID/stedi-tests-michael:latest
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: ".5"
              memory: "500Mi"
            requests:
              cpu: ".25"
              memory: "250Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 3